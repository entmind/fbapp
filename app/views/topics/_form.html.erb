<%= form_for(@topic) do |f| %>
  <%= f.error_messages! %>

  <div class="field">
    <%= f.label "トピック" %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "トピックを送信する", class: 'btn btn-md btn-success' %>
  </div>
<% end %>
